# DevPulse capstone — runtime dependencies
# Install with: pip install -r requirements.txt

# Core MCP + HTTP
mcp>=1.0.0
httpx>=0.27.0

# Agent + orchestration
langgraph>=0.2.0

# LLM clients (OpenAI-compatible interface used for both Ollama and Groq paths)
openai>=1.40.0
groq>=0.11.0

# A2A Agent Card (pydantic models)
pydantic>=2.0.0

# Misc
python-dotenv>=1.0.0
rich>=13.0.0
